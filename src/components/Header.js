import React from "react";

function Header(){
    return(
        <nav className="nav-bar">
            <img src="airbnb-logo.png" className="logo" alt="airbnb"/>
        </nav>
    );
}

export default Header;